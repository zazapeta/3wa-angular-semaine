import { PastryTagColorPipe } from './pastry-tag-color.pipe';

describe('PastryTagColorPipe', () => {
  it('create an instance', () => {
    const pipe = new PastryTagColorPipe();
    expect(pipe).toBeTruthy();
  });
});
